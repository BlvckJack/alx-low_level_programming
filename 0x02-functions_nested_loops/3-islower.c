#include "main.h"

/**
 * _islower - checks if a character is lowercase
 * @c: charater to be checked
 *
 * Return: 1 if lower, 0 otherwise
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
